# AGENTS.md

Bare Python project scaffolded by PyCharm. Single file: `main.py` (auto-generated template). No build system, tests, linting, or dependencies configured.

## Run

```
python main.py
```

## Notes

- `.venv/` exists but no dependencies are installed beyond the default Python stdlib.
- `.idea/` is the PyCharm project config — do not modify manually.

## Agent skills

### Issue tracker

Issues and specs live as local markdown files under `.scratch/<feature>/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` at root + `docs/adr/` for ADRs. See `docs/agents/domain.md`.
