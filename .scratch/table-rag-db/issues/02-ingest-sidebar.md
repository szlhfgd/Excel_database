# 02: 文件导入 + 侧边栏表管理

**What to build:** 端到端导入链路：用户从侧边栏上传 Excel/CSV（CSV 自动探测 UTF-8/GBK 编码），系统清洗（去空行、统一列名）、按文件名建表、写入行；侧边栏展示已导入表列表并提供多选（默认全选）以控制搜索作用域。验证：上传后表出现在列表且可勾选。

**Blocked by:** 01（项目骨架与 SQLite 底座）

**Status:** ready-for-agent

- [ ] 上传 `.xlsx` 成功导入为一张表，表名取文件名去扩展名
- [ ] 上传 `.csv` 成功导入，编码依次尝试 UTF-8 → GBK/GB18030 自动探测
- [ ] 导入时清洗空行、统一列名（去空格/特殊字符）
- [ ] 重新导入同名文件覆盖旧数据（先删后导）
- [ ] 侧边栏列出所有已导入表，提供多选控件，默认全选
